apiVersion: v1
kind: Pod
metadata:
  name: test-insecure
  namespace: default
spec:
  hostPID: true
  containers:
    - name: app
      image: nginx:latest
      securityContext:
        privileged: true
        allowPrivilegeEscalation: true
      ports:
        - containerPort: 80
          hostPort: 8080
